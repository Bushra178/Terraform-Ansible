#!/bin/bash
# exec > /tmp/user_data.log 2>&1
# sudo yum update -y 
# sudo yum install docker -y
# echo "updated and installed docker...."
# sudo systemctl start docker
# echo "started docker"s
# sudo usermod -aG docker ec2-user
# docker run -p 8080:80 nginx
# echo "running nginx"
